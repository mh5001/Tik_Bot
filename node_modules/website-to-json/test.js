var wtj = require('./index')

wtj.extractData(process.env.DOMAIN, {
  //links: true
  headers: true,
  debug: true,
  emails: false,
  forever: false
  //http: true
})
.then(function(res) {
  console.log(JSON.stringify(res, null, 2));
})
